version: '3.6'
services:
  #----Start: EIS Containers ----

  # Define VideoIngestion service

  ia_video_ingestion2:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion2
    hostname: ia_video_ingestion2
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion2"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera2_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera2_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion3:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion3
    hostname: ia_video_ingestion3
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion3"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera3_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera3_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion4:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion4
    hostname: ia_video_ingestion4
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion4"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera4_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera4_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion5:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion5
    hostname: ia_video_ingestion5
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion5"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera5_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera5_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion6:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion6
    hostname: ia_video_ingestion6
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion6"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera6_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera6_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion7:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion7
    hostname: ia_video_ingestion7
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion7"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera7_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera7_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion8:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion8
    hostname: ia_video_ingestion8
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion8"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera8_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera8_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion9:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion9
    hostname: ia_video_ingestion9
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion9"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera9_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera9_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion10:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion10
    hostname: ia_video_ingestion10
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion10"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera10_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera10_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion11:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion11
    hostname: ia_video_ingestion11
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion11"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera11_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera11_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion12:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion12
    hostname: ia_video_ingestion12
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion12"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera12_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera12_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion13:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion13
    hostname: ia_video_ingestion13
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion13"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera13_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera13_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion14:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion14
    hostname: ia_video_ingestion14
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion14"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera14_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera14_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion15:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion15
    hostname: ia_video_ingestion15
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion15"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera15_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera15_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion16:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion16
    hostname: ia_video_ingestion16
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion16"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera16_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera16_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion17:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion17
    hostname: ia_video_ingestion17
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion17"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera17_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera17_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion18:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion18
    hostname: ia_video_ingestion18
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion18"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera18_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera18_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion19:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion19
    hostname: ia_video_ingestion19
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion19"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera19_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera19_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion20:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion20
    hostname: ia_video_ingestion20
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion20"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera20_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera20_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion21:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion21
    hostname: ia_video_ingestion21
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion21"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera21_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera21_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion22:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion22
    hostname: ia_video_ingestion22
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion22"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera22_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera22_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion23:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion23
    hostname: ia_video_ingestion23
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion23"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera23_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera23_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion24:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion24
    hostname: ia_video_ingestion24
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion24"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera24_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera24_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  ia_video_ingestion25:
    image: ${DOCKER_REGISTRY}ia_video_ingestion:${EIS_VERSION}
    container_name: ia_video_ingestion25
    hostname: ia_video_ingestion25
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoIngestion25"
      # Setting Visualizer as a subscriber here since FpsCalculator uses
      # the same keys as visualizer to subscribe over both VideoIngestion
      # and VideoAnalytics
      Clients: "VideoAnalytics"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      no_proxy: ${no_proxy},${RTSP_CAMERA_IP}
      SOCKET_DIR: ${SOCKET_DIR}
      PubTopics: "camera25_stream"
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolut_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera25_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
    volumes:
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    privileged: true
    devices:
      - "/dev/dri"
      - "/dev/:/dev/"
    user: ${EIS_UID}
    secrets:
      - ca_etcd
      - etcd_VideoIngestion_cert
      - etcd_VideoIngestion_key

  # Define VideoAnalytics service

  ia_video_analytics2:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics2
    hostname: ia_video_analytics2
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera2_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera2_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera2_stream_results"
      camera2_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics3:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics3
    hostname: ia_video_analytics3
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera3_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera3_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera3_stream_results"
      camera3_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics4:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics4
    hostname: ia_video_analytics4
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera4_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera4_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera4_stream_results"
      camera4_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics5:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics5
    hostname: ia_video_analytics5
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera5_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera5_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera5_stream_results"
      camera5_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics6:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics6
    hostname: ia_video_analytics6
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera6_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera6_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera6_stream_results"
      camera6_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics7:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics7
    hostname: ia_video_analytics7
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera7_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera7_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera7_stream_results"
      camera7_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics8:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics8
    hostname: ia_video_analytics8
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera8_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera8_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera8_stream_results"
      camera8_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics9:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics9
    hostname: ia_video_analytics9
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera1_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera9_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera9_stream_results"
      camera9_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics10:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics10
    hostname: ia_video_analytics10
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera10_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera10_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera10_stream_results"
      camera10_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics11:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics11
    hostname: ia_video_analytics11
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera11_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera11_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera11_stream_results"
      camera11_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics12:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics12
    hostname: ia_video_analytics12
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera12_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera12_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera12_stream_results"
      camera12_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics13:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics13
    hostname: ia_video_analytics13
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera13_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera13_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera13_stream_results"
      camera13_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics14:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics14
    hostname: ia_video_analytics14
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera14_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera14_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera14_stream_results"
      camera14_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics15:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics15
    hostname: ia_video_analytics15
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera15_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera15_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera15_stream_results"
      camera15_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics16:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics16
    hostname: ia_video_analytics16
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera16_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera16_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera16_stream_results"
      camera16_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics17:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics17
    hostname: ia_video_analytics17
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera17_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera17_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera17_stream_results"
      camera17_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics18:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics18
    hostname: ia_video_analytics18
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera18_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera18_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera18_stream_results"
      camera18_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics19:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics19
    hostname: ia_video_analytics19
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera19_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera19_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera19_stream_results"
      camera19_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics20:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics20
    hostname: ia_video_analytics20
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera20_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera20_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera20_stream_results"
      camera20_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics21:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics21
    hostname: ia_video_analytics21
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera21_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera21_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera21_stream_results"
      camera21_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics22:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics22
    hostname: ia_video_analytics22
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera22_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera22_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera22_stream_results"
      camera22_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics23:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics23
    hostname: ia_video_analytics23
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera23_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera23_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera23_stream_results"
      camera23_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics24:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics24
    hostname: ia_video_analytics24
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera24_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera24_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera24_stream_results"
      camera24_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key

  ia_video_analytics25:
    image: ${DOCKER_REGISTRY}ia_video_analytics:${EIS_VERSION}
    container_name: ia_video_analytics25
    hostname: ia_video_analytics25
    network_mode: host
    restart: unless-stopped
    environment:
      AppName: "VideoAnalytics"
      Clients : "Visualizer,FactoryControlApp,OpcuaExport,ImageStore,InfluxDBConnector"
      CertType: "zmq"
      DEV_MODE: ${DEV_MODE}
      PROFILING_MODE: ${PROFILING_MODE}
      SubTopics: "VideoIngestion/camera25_stream"
      SOCKET_DIR: ${SOCKET_DIR}
      # Use IPC mode (zmq_ipc) when VideoIngestion and VideoAnalytics based
      # containers are running on the same host. If they are running on diff
      # host, please use TCP mode for communication.
      # Eg: Stream cfg for
      # IPC: zmq_ipc, <absolute_socket_directory_path>
      # TPC: zmq_tcp, <publisher_host>:<publisher_port>
      camera25_stream_cfg: "zmq_ipc,${SOCKET_DIR}/"
      PubTopics: "camera25_stream_results"
      camera25_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
      no_proxy: ${no_proxy}
      # camera1_stream_results_cfg: "zmq_ipc,${SOCKET_DIR}/"
    privileged: true
    volumes:
      - "/dev:/dev"
      - "$PWD/test_videos:/EIS/test_videos"
      - "${EIS_INSTALL_PATH}/sockets:${SOCKET_DIR}"
    devices:
      - "/dev/dri"
    secrets:
      - ca_etcd
      - etcd_VideoAnalytics_cert
      - etcd_VideoAnalytics_key
